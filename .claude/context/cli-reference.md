# STOA CLI-First Reference

> Auto-generated by `scripts/generate-cli-context.sh`. Do not edit manually.
> Regenerate after any stoactl command or schema change.
>
> **Purpose**: This file provides the complete STOA API surface via stoactl commands
> and JSON Schemas. Use this instead of reading `control-plane-api/src/` when you
> need to understand what resources exist and how to manipulate them.

## Quick Decision Tree

```
Need to know what resources exist?     → "Resource Kinds" section below
Need to create/update a resource?      → stoactl apply -f <yaml>
Need to list resources?                → stoactl get <resource>
Need to delete a resource?             → stoactl delete <kind> <id>
Need resource field details?           → "Resource Schemas" section below
Need to check system health?           → stoactl doctor
Need API endpoint for code?            → "Endpoint Map" section below
```

## Commands

```
stoactl is a GitOps-native CLI for managing STOA Platform resources.
It provides a declarative, kubectl-like experience for managing APIs,
subscriptions, and other STOA resources through infrastructure-as-code patterns.
Example usage:
  stoactl config set-context prod --server=https://api.gostoa.dev --tenant=acme
  stoactl config use-context prod
  stoactl auth login
  stoactl get apis
  stoactl apply -f api.yaml
Admin mode:
  stoactl --admin get apis         # Uses service account token instead of user OIDC
Usage:
  stoactl [command]
Available Commands:
  apply        Apply a configuration to a resource by file
  audit        Query and export audit logs
  auth         Manage authentication
  bridge       Generate Tool CRDs from an OpenAPI spec
  catalog      Manage API catalog synchronization
  completion   Generate shell completion scripts
  config       Manage stoactl configuration
  connect      Manage stoa-connect gateway agents
  delete       Delete resources
  deploy       Deploy an API or manage deployments
  doctor       Check system health and prerequisites
  gateway      Manage gateway instances
  get          Display one or many resources
  help         Help about any command
  init         Initialize a new STOA project
  logs         Show deployment history for an API
  mcp          Interact with MCP servers and tools
  subscription Manage subscriptions
  tenant       Manage tenants
  token-usage  Display token consumption for the current tenant
  version      Print the version information
Flags:
      --admin   Use service account token instead of user OIDC token
  -h, --help    help for stoactl
Use "stoactl [command] --help" for more information about a command.
```

### stoactl get

```
Display one or many resources.
Prints a table of the most important information about the specified resources.
You can filter the list using a NAME or use -o for different output formats.
Examples:
  # List all APIs in table format
  stoactl get apis
  # Get a specific API
  stoactl get api billing-api
  # List APIs in wide format (more columns)
  stoactl get apis -o wide
  # Get API as YAML
  stoactl get api billing-api -o yaml
  # Get APIs as JSON
  stoactl get apis -o json
Usage:
  stoactl get [command]
Available Commands:
  apis             Display APIs
  consumers        Display consumers
  contracts        Display Universal API Contracts
  environments     Display environments
  gateways         Display gateway instances
  plans            Display subscription plans
  service-accounts Display service accounts
  subscriptions    Display subscriptions
  tenants          Display tenants
  webhooks         Display webhook configurations
Flags:
  -h, --help            help for get
  -o, --output string   Output format: table, wide, yaml, json (default "table")
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl get [command] --help" for more information about a command.
```

### stoactl apply

```
Apply a configuration to a resource by file.
The resource will be created if it doesn't exist, or updated if it does.
This command is idempotent and safe to run multiple times.
Supported kinds: API, Tenant, Gateway, Subscription, Consumer, Contract,
MCPServer, ServiceAccount, Plan, Webhook.
Examples:
  # Apply an API definition
  stoactl apply -f api.yaml
  # Apply a consumer
  stoactl apply -f consumer.yaml
  # Apply multiple resources from a directory
  stoactl apply -f ./manifests/
  # Dry-run to validate without applying
  stoactl apply -f api.yaml --dry-run
Usage:
  stoactl apply [flags]
Flags:
      --dry-run       Validate without applying changes
  -f, --file string   Path to YAML file or directory (required)
  -h, --help          help for apply
Global Flags:
      --admin   Use service account token instead of user OIDC token
```

### stoactl delete

```
Delete resources by name or ID.
Examples:
  # Delete an API
  stoactl delete api billing-api
  # Delete multiple APIs
  stoactl delete api billing-api payments-api
  # Delete a consumer
  stoactl delete consumer abc-123
  # Delete a contract
  stoactl delete contract my-contract-id
Usage:
  stoactl delete [command]
Available Commands:
  api             Delete one or more APIs
  consumer        Delete one or more consumers
  contract        Delete one or more contracts
  gateway         Delete one or more gateway instances
  plan            Delete one or more subscription plans
  service-account Delete one or more service accounts
  subscription    Delete one or more subscriptions
  tenant          Delete one or more tenants
  webhook         Delete one or more webhooks
Flags:
  -h, --help   help for delete
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl delete [command] --help" for more information about a command.
```

### stoactl catalog

```
Manage the API catalog — trigger syncs, view stats, and inspect APIs.
Examples:
  # Show catalog stats and drift (dry-run, default)
  stoactl catalog sync
  # Trigger an actual sync
  stoactl catalog sync --apply
  # Sync a specific tenant
  stoactl catalog sync --apply --tenant acme
  # View stats as JSON
  stoactl catalog sync -o json
Usage:
  stoactl catalog [command]
Available Commands:
  sync        Show catalog drift or trigger synchronization
Flags:
  -h, --help   help for catalog
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl catalog [command] --help" for more information about a command.
```

### stoactl audit

```
Query and export audit trail entries for compliance and investigation.
Examples:
  # Export last 7 days for tenant (PII redacted by default)
  stoactl audit export --tenant acme --since 7d
  # Export last 30 days as CSV
  stoactl audit export --tenant acme --since 30d -o csv
  # Export with full PII (shows warning)
  stoactl audit export --tenant acme --since 7d --redact-pii=false
  # Filter by action
  stoactl audit export --tenant acme --since 7d --action api_call
Usage:
  stoactl audit [command]
Available Commands:
  export      Export audit log entries
Flags:
  -h, --help   help for audit
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl audit [command] --help" for more information about a command.
```

### stoactl gateway

```
Manage STOA Platform gateway instances.
Examples:
  stoactl gateway list
  stoactl gateway get <id>
  stoactl gateway health <id>
Usage:
  stoactl gateway [command]
Aliases:
  gateway, gw, gateways
Available Commands:
  get         Get a gateway instance by ID
  health      Check gateway health
  list        List all gateway instances
Flags:
  -h, --help            help for gateway
  -o, --output string   Output format: table, wide, yaml, json (default "table")
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl gateway [command] --help" for more information about a command.
```

### stoactl mcp

```
Manage MCP (Model Context Protocol) servers and tools on the STOA Platform.
By default, commands talk to the Control Plane API (source of truth).
Use --gateway to bypass the CP API and hit a gateway directly (for debugging).
Examples:
  stoactl mcp list-servers
  stoactl mcp list-tools --server petstore
  stoactl mcp call list-pets --server petstore --input '{"limit": 5}'
  stoactl mcp health
  stoactl mcp health --gateway http://localhost:30080
Usage:
  stoactl mcp [command]
Available Commands:
  call         Invoke an MCP tool
  health       Check MCP health status
  list-servers List registered MCP servers
  list-tools   List MCP tools
Flags:
      --gateway string   Gateway URL for direct access (bypasses CP API RBAC)
  -h, --help             help for mcp
  -o, --output string    Output format: table, json, yaml (default "table")
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl mcp [command] --help" for more information about a command.
```

### stoactl bridge

```
Bridge converts OpenAPI 3.x operations into STOA Tool CRD resources.
Each operation becomes a Tool CRD YAML file, ready for 'stoactl apply -f'.
This enables any REST API to be exposed as MCP tools through the STOA Gateway.
Examples:
  # Generate Tool CRDs from a Petstore spec
  stoactl bridge petstore.yaml --namespace tenant-acme
  # Preview without writing files
  stoactl bridge petstore.yaml --namespace tenant-acme --dry-run
  # Filter by tags
  stoactl bridge petstore.yaml --namespace tenant-acme --include-tags payments
  # Override server URL
  stoactl bridge petstore.yaml --namespace tenant-acme --server https://api.internal.com
  # Apply directly to gateway
  stoactl bridge petstore.yaml --namespace tenant-acme --apply
Usage:
  stoactl bridge <spec-file> [flags]
Flags:
      --apply                  Apply tools directly to gateway via API
      --auth-secret string     Secret name for authentication (generates secretRef)
      --dry-run                Parse and map, show summary, don't write files
      --exclude-tags strings   Exclude operations with these tags
  -h, --help                   help for bridge
      --include-ops strings    Only include these operationIds
      --include-tags strings   Only include operations with these tags
  -n, --namespace string       Target namespace for generated tools (required)
  -o, --output string          Output directory for generated YAML files (default "./tools/")
      --server string          Override servers[0].url from spec
      --server-name string     MCP server name for --apply (default: derived from spec title)
      --timeout string         Default timeout for generated tools (default "30s")
Global Flags:
      --admin   Use service account token instead of user OIDC token
```

### stoactl auth

```
Authenticate with STOA Platform and manage credentials.
Usage:
  stoactl auth [command]
Available Commands:
  login       Authenticate with STOA Platform
  logout      Log out from STOA Platform
  rotate-key  Rotate the stored API key
  status      Display authentication status
Flags:
  -h, --help   help for auth
Global Flags:
      --admin   Use service account token instead of user OIDC token
Use "stoactl auth [command] --help" for more information about a command.
```

## Resource Schemas (gostoa.dev/v1beta1)

All resources follow the Kubernetes-style structure:

```yaml
apiVersion: gostoa.dev/v1beta1
kind: <Kind>
metadata:
  name: <unique-name>
  namespace: <tenant-id>  # optional
  labels: {}              # optional key-value pairs
spec:
  # kind-specific fields below
```

### API

```yaml
kind: API
spec:
  name: string  # required
  display_name: string
  description: string
  backend_url: string  # required
  openapi_spec_url: string
  auth_type: none | api_key | bearer | basic | oauth2_cc  # default: none
  auth_config: object
```

### Consumer

```yaml
kind: Consumer
spec:
  external_id: string  # required
  name: string  # required
  email: string  # required
  company: string
  description: string
  keycloak_user_id: string
  consumer_metadata: object
```

### Contract

```yaml
kind: Contract
spec:
  name: string  # required
  display_name: string
  description: string
  version: string  # default: 1.0.0
  openapi_spec_url: string
```

### Gateway

```yaml
kind: Gateway
spec:
  name: string  # required
  display_name: string  # required
  gateway_type: string  # required
  environment: string  # required
  tenant_id: string
  base_url: string  # required
  auth_config: object
  capabilities: array
  tags: array
```

### MCPServer

```yaml
kind: MCPServer
spec:
  name: string  # required
  display_name: string  # required
  description: string  # required
  icon: string
  category: platform | tenant | public  # default: public
  tenant_id: string
  visibility: MCPServerVisibility  # default: {'roles': None, 'exclude_roles': None, 'public': True}
  requires_approval: boolean  # default: False
  auto_approve_roles: array
  version: string
  documentation_url: string
```

### Plan

```yaml
kind: Plan
spec:
  slug: string  # required
  name: string  # required
  description: string
  rate_limit_per_second: integer
  rate_limit_per_minute: integer
  daily_request_limit: integer
  monthly_request_limit: integer
  burst_limit: integer
  requires_approval: boolean  # default: False
  auto_approve_roles: array
  pricing_metadata: object
```

### ServiceAccount

```yaml
kind: ServiceAccount
spec:
  name: string  # required
  description: string
```

### Subscription

```yaml
kind: Subscription
spec:
  application_id: string  # required
  application_name: string  # required
  api_id: string  # required
  api_name: string  # required
  api_version: string  # required
  tenant_id: string  # required
  plan_id: string
  plan_name: string  # default: default
```

### Tenant

```yaml
kind: Tenant
spec:
  name: string  # required
  display_name: string  # required
  description: string  # default: 
  owner_email: string  # required
```

### Webhook

```yaml
kind: Webhook
spec:
  name: string  # required
  url: string  # required
  events: array  # required
  secret: string
  headers: object
```

## Endpoint Map (stoactl command -> CP API)

| stoactl command | HTTP Method | CP API Endpoint |
|-----------------|-------------|-----------------|
| `get apis` | GET | `/v1/tenants/{t}/apis` |
| `get tenants` | GET | `/v1/tenants` |
| `get gateways` | GET | `/v1/gateways` |
| `get subscriptions` | GET | `/v1/subscriptions/{t}` |
| `get consumers` | GET | `/v1/consumers/{t}` |
| `get contracts` | GET | `/v1/tenants/{t}/contracts` |
| `get service-accounts` | GET | `/v1/service-accounts` |
| `get environments` | GET | `/v1/environments` |
| `get plans` | GET | `/v1/plans/{t}` |
| `get webhooks` | GET | `/v1/tenants/{t}/webhooks` |
| `apply -f` (API) | POST | `/v1/tenants/{t}/apis` |
| `apply -f` (Tenant) | POST | `/v1/tenants` |
| `apply -f` (Gateway) | POST | `/v1/gateways` |
| `apply -f` (Subscription) | POST | `/v1/subscriptions` |
| `apply -f` (Consumer) | POST | `/v1/consumers/{t}` |
| `apply -f` (Contract) | POST | `/v1/tenants/{t}/contracts` |
| `apply -f` (MCPServer) | POST | `/v1/mcp-servers` |
| `apply -f` (ServiceAccount) | POST | `/v1/service-accounts` |
| `apply -f` (Plan) | POST | `/v1/plans/{t}` |
| `apply -f` (Webhook) | POST | `/v1/tenants/{t}/webhooks` |
| `delete <kind> <id>` | DELETE | `/v1/<resource>/{id}` |
| `catalog sync --dry-run` | GET | `/v1/admin/catalog/drift` |
| `catalog sync --apply` | POST | `/v1/admin/catalog/sync` |
| `audit export` | GET | `/v1/audit` |
| `gateway health` | GET | `/health` (gateway direct) |
| `mcp list-tools` | GET | `/mcp/tools/list` |
| `mcp list-servers` | GET | `/v1/mcp-servers` |
| `mcp call` | POST | `/mcp/tools/call` |
| `bridge` | N/A | Local OpenAPI→CRD conversion |
| `doctor` | GET | Multiple health endpoints |
| `auth status` | GET | `/v1/auth/me` |

## Gateway Types (enum)

`webmethods`, `kong`, `apigee`, `aws_apigateway`, `azure_apim`, `gravitee`,
`stoa`, `stoa_edge_mcp`, `stoa_sidecar`, `stoa_proxy`, `stoa_shadow`

## Auth Types (API backend)

`none`, `api_key`, `bearer`, `basic`, `oauth2_cc`

## MCP Server Categories (enum)

`platform`, `tenant`, `public`

## MCP Server Visibility

Complex object: `{ roles: string[] | null, exclude_roles: string[] | null, public: boolean }`

---
*Generated: 2026-04-12T10:44:51Z | Schemas: 10 kinds | apiVersion: gostoa.dev/v1beta1*
