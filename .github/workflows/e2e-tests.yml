name: E2E Tests

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'e2e/**'
      - 'portal/**'
      - 'control-plane-ui/**'
      - '.github/workflows/e2e-tests.yml'

  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'e2e/**'
      - 'portal/**'
      - 'control-plane-ui/**'

  # Run daily at 6 AM UTC
  schedule:
    - cron: '0 6 * * *'

  # Manual trigger with test suite selection
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Test suite to run'
        required: false
        default: 'smoke'
        type: choice
        options:
          - all
          - smoke
          - critical
          - portal
          - console
          - gateway
          - rpo
      environment:
        description: 'Target environment'
        required: false
        default: 'staging'
        type: choice
        options:
          - dev
          - staging
          - production

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read
  id-token: write  # Required for Vault OIDC authentication

env:
  NODE_VERSION: '20'

jobs:
  e2e-tests:
    name: E2E Tests (shard ${{ matrix.shard }}/3)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # Scheduled runs are non-blocking — environment data may be incomplete
    continue-on-error: ${{ github.event_name == 'schedule' }}
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Fetch E2E secrets from Vault
        id: vault-secrets
        continue-on-error: true  # Gracefully fall back to GitHub secrets if Vault fails
        uses: hashicorp/vault-action@v3
        with:
          url: https://vault.gostoa.dev
          method: jwt
          role: github-actions
          jwtGithubAudience: sigstore
          exportEnv: true
          secrets: |
            secret/data/e2e/urls PORTAL_URL | E2E_PORTAL_URL ;
            secret/data/e2e/urls CONSOLE_URL | E2E_CONSOLE_URL ;
            secret/data/e2e/urls GATEWAY_URL | E2E_GATEWAY_URL ;
            secret/data/e2e/urls KEYCLOAK_URL | E2E_KEYCLOAK_URL ;
            secret/data/e2e/personas/parzival username | PARZIVAL_USER ;
            secret/data/e2e/personas/parzival password | PARZIVAL_PASSWORD ;
            secret/data/e2e/personas/art3mis username | ART3MIS_USER ;
            secret/data/e2e/personas/art3mis password | ART3MIS_PASSWORD ;
            secret/data/e2e/personas/aech username | AECH_USER ;
            secret/data/e2e/personas/aech password | AECH_PASSWORD ;
            secret/data/e2e/personas/sorrento username | SORRENTO_USER ;
            secret/data/e2e/personas/sorrento password | SORRENTO_PASSWORD ;
            secret/data/e2e/personas/i-r0k username | I_R0K_USER ;
            secret/data/e2e/personas/i-r0k password | I_R0K_PASSWORD ;
            secret/data/e2e/personas/anorak username | ANORAK_USER ;
            secret/data/e2e/personas/anorak password | ANORAK_PASSWORD ;
            secret/data/e2e/personas/alex username | ALEX_USER ;
            secret/data/e2e/personas/alex password | ALEX_PASSWORD ;
            secret/data/e2e/api-keys test | TEST_API_KEY

      - name: Setup E2E environment
        uses: ./.github/actions/e2e-setup

      - name: Run E2E tests
        working-directory: e2e
        env:
          # URLs - Vault values take precedence, then secrets, then defaults
          STOA_PORTAL_URL: ${{ env.E2E_PORTAL_URL || secrets.E2E_PORTAL_URL || 'https://portal.gostoa.dev' }}
          STOA_CONSOLE_URL: ${{ env.E2E_CONSOLE_URL || secrets.E2E_CONSOLE_URL || 'https://console.gostoa.dev' }}
          STOA_GATEWAY_URL: ${{ env.E2E_GATEWAY_URL || secrets.E2E_GATEWAY_URL || 'https://api.gostoa.dev' }}
          KEYCLOAK_URL: ${{ env.E2E_KEYCLOAK_URL || secrets.E2E_KEYCLOAK_URL || 'https://auth.gostoa.dev' }}
          KEYCLOAK_REALM: stoa

          # Personas - High-Five (Vault values take precedence)
          PARZIVAL_USER: ${{ env.PARZIVAL_USER || secrets.PARZIVAL_USER }}
          PARZIVAL_PASSWORD: ${{ env.PARZIVAL_PASSWORD || secrets.PARZIVAL_PASSWORD }}
          ART3MIS_USER: ${{ env.ART3MIS_USER || secrets.ART3MIS_USER }}
          ART3MIS_PASSWORD: ${{ env.ART3MIS_PASSWORD || secrets.ART3MIS_PASSWORD }}
          AECH_USER: ${{ env.AECH_USER || secrets.AECH_USER }}
          AECH_PASSWORD: ${{ env.AECH_PASSWORD || secrets.AECH_PASSWORD }}

          # Personas - IOI
          SORRENTO_USER: ${{ env.SORRENTO_USER || secrets.SORRENTO_USER }}
          SORRENTO_PASSWORD: ${{ env.SORRENTO_PASSWORD || secrets.SORRENTO_PASSWORD }}
          I_R0K_USER: ${{ env.I_R0K_USER || secrets.I_R0K_USER }}
          I_R0K_PASSWORD: ${{ env.I_R0K_PASSWORD || secrets.I_R0K_PASSWORD }}

          # Personas - Admin
          ANORAK_USER: ${{ env.ANORAK_USER || secrets.ANORAK_USER }}
          ANORAK_PASSWORD: ${{ env.ANORAK_PASSWORD || secrets.ANORAK_PASSWORD }}

          # Personas - Freelance
          ALEX_USER: ${{ env.ALEX_USER || secrets.ALEX_USER }}
          ALEX_PASSWORD: ${{ env.ALEX_PASSWORD || secrets.ALEX_PASSWORD }}

          # Test API Key
          TEST_API_KEY: ${{ env.TEST_API_KEY || secrets.TEST_API_KEY }}

        run: |
          TEST_SUITE="${{ github.event.inputs.test_suite || 'smoke' }}"

          # Always run auth-setup first to generate storage state files
          echo "Running auth-setup to generate storage state..."
          npx playwright test --project=auth-setup || {
            echo "Auth setup failed - check Keycloak credentials"
            exit 1
          }

          # Then run the filtered tests
          SHARD="--shard=${{ matrix.shard }}/3"

          case "$TEST_SUITE" in
            "all")
              npx playwright test $SHARD
              ;;
            "smoke")
              npx playwright test --grep @smoke $SHARD
              ;;
            "critical")
              npx playwright test --grep @critical $SHARD
              ;;
            "portal")
              npx playwright test --grep @portal $SHARD
              ;;
            "console")
              npx playwright test --grep @console $SHARD
              ;;
            "gateway")
              npx playwright test --grep @gateway $SHARD
              ;;
            "rpo")
              npx playwright test --grep @rpo $SHARD
              ;;
            *)
              npx playwright test --grep @smoke $SHARD
              ;;
          esac

      - name: Upload HTML report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: always()
        with:
          name: playwright-report-shard-${{ matrix.shard }}-${{ github.run_id }}
          path: e2e/reports/html/
          retention-days: 14

      - name: Upload test results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: failure()
        with:
          name: test-results-shard-${{ matrix.shard }}-${{ github.run_id }}
          path: |
            e2e/test-results/
            e2e/reports/results.json
          retention-days: 7

      - name: Post test summary
        if: always()
        run: |
          if [ -f e2e/reports/results.json ]; then
            echo "## E2E Test Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # intentional: results.json may not exist if tests crashed before producing output
            TOTAL=$(jq '.stats.expected + .stats.unexpected + .stats.flaky + .stats.skipped' e2e/reports/results.json 2>/dev/null || echo "0")
            PASSED=$(jq '.stats.expected' e2e/reports/results.json 2>/dev/null || echo "0")
            FAILED=$(jq '.stats.unexpected' e2e/reports/results.json 2>/dev/null || echo "0")
            SKIPPED=$(jq '.stats.skipped' e2e/reports/results.json 2>/dev/null || echo "0")

            echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Total | $TOTAL |" >> $GITHUB_STEP_SUMMARY
            echo "| Passed | $PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| Failed | $FAILED |" >> $GITHUB_STEP_SUMMARY
            echo "| Skipped | $SKIPPED |" >> $GITHUB_STEP_SUMMARY
          else
            echo "## E2E Test Results" >> $GITHUB_STEP_SUMMARY
            echo "No results file found" >> $GITHUB_STEP_SUMMARY
          fi
