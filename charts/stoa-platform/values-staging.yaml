# Staging environment overrides
global:
  environment: staging
  domain: gostoa.dev
  domainPrefix: staging-

replicaCount: 2

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi

postgresql:
  primary:
    persistence:
      size: 5Gi

# Keycloak replicas — REFERENCE ONLY (managed by stoa-infra charts/keycloak/)
# Clustering (JGroups DNS_PING) enabled when replicas > 1 (CAB-1956)
keycloak:
  replicas: 2

observability:
  prometheus:
    retention: 7d
  loki:
    retention: 14d

ingress:
  tls:
    enabled: true
    issuer: letsencrypt-staging

# STOA Gateway (Rust) — Staging overrides
stoaGateway:
  enabled: true
  replicas: 2
  mode: edge-mcp
  environment: staging
  keycloakUrl: "https://staging-auth.gostoa.dev"
  logLevel: info
  policies:
    enabled: true
  k8s:
    enabled: true
  kafka:
    enabled: true
    brokers: "redpanda.stoa-system.svc.cluster.local:9092"
  ingress:
    enabled: true
    host: staging-mcp.gostoa.dev
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-staging
    tls:
      - secretName: stoa-gateway-tls
        hosts:
          - staging-mcp.gostoa.dev
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
    limits:
      cpu: 500m
      memory: 256Mi
