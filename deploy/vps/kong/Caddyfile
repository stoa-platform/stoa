# Kong VPS — TLS termination via Caddy (Let's Encrypt)
# Ports: 8001 (admin), 8000 (proxy), 8090 (stoa-connect), 9200 (stoa-link)

kong.gostoa.dev {
	reverse_proxy localhost:8001
}

connect-kong.gostoa.dev {
	reverse_proxy localhost:8090
}

link-kong.gostoa.dev {
	reverse_proxy localhost:9200
}
