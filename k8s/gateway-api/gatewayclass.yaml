# GatewayClass — declares NGINX Gateway Fabric as a Gateway API implementation.
# This is the cluster-wide "provider" that Gateway resources reference.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: nginx
spec:
  controllerName: gateway.nginx.org/nginx-gateway-controller
