# STOA Platform - Keycloak Login Theme
# =========================================
# Custom FreeMarker templates + CSS (matches prod auth.gostoa.dev)
# KC 26.5.3 — uses keycloak (v1) parent for FreeMarker template support

parent=keycloak

# Theme metadata
meta=viewport==width=device-width,initial-scale=1

# Styles
styles=css/stoa-login.css

# Locales
locales=en,fr
