# STOA Platform - Keycloak Login Theme v2
# =========================================
# Migrated from keycloak (v1) to keycloak.v2 (CAB-1960)
# KC 26.5.3 — v2 uses server-rendered HTML (no FreeMarker templates)

parent=keycloak.v2

# Theme metadata
meta=viewport==width=device-width,initial-scale=1

# Styles — PatternFly overrides for STOA dark theme
styles=css/stoa-login.css

# Locales
locales=en,fr
