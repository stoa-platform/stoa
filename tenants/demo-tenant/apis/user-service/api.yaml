apiVersion: stoa.cab-i.com/v1
kind: API
metadata:
  name: user-service
  version: "2.1.0"
  labels:
    managed-by: gitops

spec:
  displayName: User Service API
  description: User management, authentication, and profile operations
  status: published
  category: platform
  tags:
    - rest
    - internal
    - authentication

  backend:
    url: https://users.internal.stoa.cab-i.com/api

  deployments:
    dev: true
    staging: false

  documentation:
    openapi: openapi.yaml
